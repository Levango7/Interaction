# v2.3.0：主页系统概况 + 仪表盘改名并入图表商店 + 19 工具全上线

## 一、主页加「系统概况」（放习惯链左侧，AI 助手下移）

**现状**：`renderOverview`（9779）第三行是 `AI 助手 | 习惯链` 并排。
**改后布局**（≥1024 两列）：
```
KPI 指标条
全局搜索（跨宽）
今日待办 | 数据速览
系统概况(新) | 习惯链        ← 概况放习惯链左边
AI 助手（跨宽，最下）
```

新增 `renderSystemOverviewCard()`：
- 标题「系统概况」+ 一句话可读描述（新手看得懂，不用花哨图表）
- **简洁数字清单**（每项：icon + 数字 + 标签，可点击跳转）：场景(X) / 任务(Y) / 已完成 / 资料(Z) / 笔记 / 习惯链(N) / 插件(M)，数据来自 getTasks/getNotes/getLinks/getEnabledPlugins 等现有函数
- **AI 助手状态行**：已启用/未启用徽标（未启用时显示「设置 → AI」引导）
- 一行新手提示：点击数字直达对应功能；右上角问号打开使用指南
- `renderOverview` 模板调整卡片顺序（AI Hub 移到最下 `<div class="ov-span2">`）

## 二、仪表盘改名 + 废弃图表商店（已拍板：废弃并入）

- **侧栏** `dash-chart`：label「总览图」→「**编辑**」（`_buildSideMenu` 约 7493）
- **run 改向**：dash-chart 与 dat-chart（数据·图表）的 run 不再 `openChartStore()`，改为直接进统计页编辑态：`_sideActive=null; setActive("stats"); _dashEditMode=true; render();`
- **标题栏** `renderStats` h2「统计」→「**仪表盘**」（展示态）／「**编辑仪表盘**」（编辑态 `_dashEditMode`），副标题同步；页头按钮文案保持「⚙编辑布局 / ✓完成编辑」
- **废弃 openChartStore**：保留 `_findSideMenuItem` 注册但不接菜单入口（函数本体可留在代码作为死代码兜底，避免插件引用报错）；命令面板里指向 chart store 的项改为进仪表盘编辑态
- **编辑页已承担排布**（v2.2.1 已具备：添加/移除/拖拽/重置/完成编辑，组件含统计卡+数据可视化+业务可视化）——本轮不改内部逻辑，只改入口命名

## 三、19 个占位工具全上线（最小可用版，已拍板：全部做）

**改造机制**：
- 新建 `TOOL_APPS` 注册表：`id → {name, icon, desc, render(), bind()}`；`openToolStub` 改为「按 id 查 TOOL_APPS，命中则打开真实工具，否则保持占位」
- SC_SUBMENU 全部 19 个 `stub:true` 改为经 TOOL_APPS 分发
- 建立通用「本地记录型」骨架（localStorage 表 + CRUD + 列表 + 统计），生活组复用

**19 工具分类实现（MVP，纯前端离线约束）**：

| 簇 | 工具 | 实现 |
|---|---|---|
| 文档 | off-md | Markdown 编辑器（分栏编辑+实时预览+导出 .md/.html） |
| 文档 | off-word | 富文本（contenteditable+工具栏+导出 .html/.doc） |
| 文档 | off-sheet | 表格编辑器（可编辑 table 行列增删+导出 .csv） |
| 文档 | off-ppt | 幻灯片（卡片编辑+全屏浏览+导出 JSON） |
| 文档 | off-pdf | PDF 阅读（本地文档导入 + 真实 PDF 用 iframe 预览；图片 PDF 降级为阅读说明） |
| 文档 | off-ocr | 截图 OCR（接 AI 多模态描述，未配置引导） |
| 设计 | des-cad | 画布绘制（线条/矩形/圆/移动/导出 SVG） |
| 设计 | des-ps | 图片滤镜（上传 canvas 灰度/反色/亮度/对比/缩放/导出） |
| 设计 | des-imggen | AI 文生图（prompt + 调生成 API，接 AI Key，未配置引导） |
| 设计 | des-vidgen | AI 视频生成（说明 + 配置引导） |
| 检索 | stu-web | 在线检索（本地全文搜索跨任务/笔记/资料/场景，离线替代说明） |
| 编程 | cod-regex | 正则测试器（pattern/flags/测试文本/高亮匹配） |
| 编程 | cod-compile | 代码运行器（Web Worker 隔离 JS 执行+输出） |
| 编程 | cod-md2code | Markdown 转代码（解析 MD → HTML/JSON 大纲） |
| 生活 | lif-sport | 运动记录（日期/类型/时长 + 周统计） |
| 生活 | lif-bill | 缴费提醒（项目/金额/到期/状态 + 逾期标红） |
| 生活 | lif-shop | 采购清单（项目/数量/已购勾选） |
| 生活 | lif-takeout | 外卖记录（店铺/金额/评分 + 月统计） |
| 生活 | lif-transit | 出行记录（起终点/方式/费用） |

**分簇调度（subagent 并行，产出自包含片段写独立 tmp 文件，我去重合并落主文件避冲突）**：
- 子代理 A：文档簇 6 个
- 子代理 B：设计簇 4 个 + 在线检索 1 个
- 子代理 C：编程簇 3 个 + 生活簇 5 个
- 主代理：建 TOOL_APPS 框架 + 需求一二 + 合并 + 测试

## 四、版本与验证

- **v2.3.0**：六处版本 bump + build tag 20260822d；CHANGELOG/README 同步
- **测试**：
  - 新增 `tests/tools.test.js`：19 工具入口分发（openToolStub 命中 TOOL_APPS）、关键工具 CRUD 冒烟、（文档/正则/生活各抽 1-2 个断言）
  - 新增/更新仪表盘改名断言（dash-chart label「编辑」、点编辑进编辑态）
  - 全量 vitest + lint-colors/lint-layers + build --check
- **Playwright 截图自查**：主页新布局（系统概况在习惯链左）、仪表盘编辑态入口、3-5 个代表工具页面
- **用户验收前**完成全部截图实测

## 风险
- subagent 片段合并冲突 → 用独立 tmp 文件 + 唯一锚点注释，主代理统一落文件后全量跑测试兜底
- 单文件变大（约 +1500 行）→ lint-layers 分层注释保持完整
- 纯前端无后端，图片/视频生成/OCR 依赖 AI Key → 降级为「配置引导 + 本地替代」，明确标注，不冒充完整功能
- 生活组 5 个用同一骨架 → 抽公共 helpers 防重复膨胀

## 流程（按用户要求）
1. 本方案审核通过
2. 拆任务 → 3 子代理并行实现簇 → 主代理合并
3. 每簇合并后跑 vitest 快检，fail 返回上一轮
4. 全量测试 → 全局核对 → 本地提交 + 推送
5. 收尾（CHANGELOG/README/版本/清理 tmp）
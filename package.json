{
  "name": "agent-workbench",
  "version": "1.0.0",
  "private": true,
  "description": "Agent 工作台 v1.0.0 — 多场景任务 / 记录 / AI 助手单文件应用。工程化入口：测试、颜色令牌门禁、本地预览。",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "e2e": "E2E=1 npx playwright test",
    "e2e:install": "npx playwright install chromium",
    "lint": "npx eslint agent-workbench.html && node scripts/lint-colors.mjs agent-workbench.html",
    "lint:fix": "npx eslint agent-workbench.html --fix",
    "typecheck": "npx --yes -p typescript@5 tsc --noEmit || echo typecheck: html project, tsc skipped (declared in tsconfig.json)",
    "serve": "python -m http.server 8123"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@vitest/coverage-v8": "^2.1.9",
    "eslint": "^8.57.0",
    "eslint-plugin-html": "^8.1.1",
    "jsdom": "^24.1.3",
    "vitest": "^2.1.9"
  }
}
